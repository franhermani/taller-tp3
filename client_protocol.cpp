#include <string>
#include "client_protocol.h"

void ClientProtocol::encodeMessage(const char *message) {
    // TODO: ...
}

std::string ClientProtocol::decodeMessage(const char *byte_msg) {
    std::string message = "Hola mundo";
    return message;
}
